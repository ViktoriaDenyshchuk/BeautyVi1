@model BeautyVi.Core.Entities.Product

<div class="container mt-5 mb-5">
    <div class="text-center">
        <h2 class="text-uppercase" style="color: #7a6f5b; font-family: 'Playfair Display', serif;">Видалити продукт</h2>
        <p class="mt-3" style="color: #4a4a4a; font-size: 1.1rem;">
            Ви впевнені, що хочете видалити продукт <strong>"@Model.Name"</strong>?
        </p>
    </div>

    <div class="text-center mt-4">
        <form asp-action="Delete" method="post" style="display: inline-block; margin-right: 10px;">
            <input type="hidden" asp-for="Id" />
            <button type="submit" class="btn"
                    style="background-color: transparent; color: #7a6f5b; border: 2px solid #7a6f5b; padding: 10px 30px; font-weight: bold; border-radius: 4px; text-transform: uppercase;">
                Так, видалити
            </button>
        </form>
        <a asp-action="Index" class="btn"
           style="background-color: transparent; color: #7a6f5b; border: 2px solid #7a6f5b; padding: 10px 30px; font-weight: bold; border-radius: 4px; text-transform: uppercase; text-decoration: none;">
            Скасувати
        </a>
    </div>
</div>
